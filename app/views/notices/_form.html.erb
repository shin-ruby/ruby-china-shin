<div id="notice" class="form">

<%= simple_form_for(@notice) do |f| %>
  <%= render "/shared/error_messages", target: @notice  %>
    <%= f.input :content %>
    <div class="form-actions">
      <%= f.submit t("common.save"), class: "btn btn-primary" %>
    </div>
<% end %>
</div>